<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ (API 31): splash sistem wajib ada, tapi ikonnya kita buat
     TRANSPARAN supaya logo launcher tidak muncul & tidak ke-crop. Latar splash
     = biru brand, lalu langsung ke layar loading Flutter. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">#2563EB</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
